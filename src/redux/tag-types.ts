export enum TagTypes {
    user = 'user',
    department = 'department',
    faculty = 'faculty',
    admin = 'admin'
}

export const tagTypeList = [
  TagTypes.user,
  TagTypes.department,
  TagTypes.faculty,
  TagTypes.admin,
];